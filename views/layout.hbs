<!DOCTYPE html>
<html>

<head>
  <title>{{title}}</title>
  <link rel="stylesheet" href="/stylesheets/main.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
  <header class="container-fluid" id="header">
    <h1 class="text text-primary">STORE HaiLong</h1>
    <nav>
      {{#if user}}
        <a class="btn btn-info" href="/">Home</a>
        <a class="btn btn-info" href="/bills">Bills</a>
        <a class="btn btn-info" href="/services">Services</a>
        <span>Welcome, {{user.username}} ({{user.role}})</span>
        <a class="btn btn-danger" href="/auth/logout">Logout</a>
      {{else}}
        <a class="btn btn-info" href="/auth/login">Login</a>
      {{/if}}
    </nav>
  </header>

  <main>
    {{{body}}}
  </main>
</body>

</html>
